<% @recipes.each do |recipe| %>

    <%= recipe.name%>
    <%= recipe.ingredients%>
    <%= recipe.cook_time%>
    
  <% recipe_link = "/recipes/#{recipe.id}" %>
    <p><a href="<%= recipe_link %>"><%= recipe.name %></a></p>
    <% end%>